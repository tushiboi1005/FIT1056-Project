
# import tkinter as tk

# root = tk.Tk()

# root.geometry("750x500")
# root.configure(bg="lightblue")




# entry = tk.Label(root,font="Arial",text="Welcome to EmpowerU!!!",bg="lightblue")
# entry.place(relx=0.4,rely=0.3,relwidth=0.3,relheight=0.1)

# username_label = tk.Label(root,font="Calibri",text="username:",bg="lightblue")
# username_label.place(relx=0.3,rely=0.5)

# username_entry = tk.Entry(root)
# username_entry.place(relx=0.4,rely=0.5)

# password_label = tk.Label(root,font="Calibri",text="password:",bg="lightblue")
# password_label.place(relx=0.3,rely=0.45)

# password_entry = tk.Entry(root)
# password_entry.place(relx=0.4,rely=0.45)







# root.mainloop()
